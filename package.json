{
  "name": "llm-server-test-harness",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "cli"
  ],
  "dependencies": {
    "better-sqlite3": "^12.5.0",
    "fastify": "^5.6.2"
  },
  "scripts": {
    "cli": "npm -w cli run cli --",
    "dev": "concurrently \"npm -w backend run dev\" \"npm -w frontend run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
